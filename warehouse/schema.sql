CREATE TABLE CORE
         (ID INT PRIMARY KEY     NOT NULL,
         symbol         CHAR(30)    NOT NULL,
         year            INT     NOT NULL,
         sector        CHAR(20),
         SALARY         REAL);